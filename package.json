{
  "devDependencies": {
    "husky": "^9.1.7"
  },

  "scripts": {
    "prepare": "husky install",
    "lint": "eslint . --fix",
    "precommit": "npm run lint && node scripts/check-todo.js",
    "test": "jest"
  }
}
